export top.zztech.ainote.model.Company
    -> package top.zztech.ainote.service.dto

input CompanyAddInput {
   #allScalars(this)
     -id
}

specification CompanySearch {
    like/i(name, code) as keywords
    like/i(contact)
    status
}