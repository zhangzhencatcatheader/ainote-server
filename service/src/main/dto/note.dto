export top.zztech.ainote.model.Note
    -> package top.zztech.ainote.service.dto

input CreateNote {
    title
    content
    id(files) as filesIds
    id(aboutAccount) as aboutAccountIds
}


specification NoteSearch {
    like/i(content,title) as keyword
}